services:
  - type: web
    name: duty-scheduler-backend
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        value: sqlite:///./duty_scheduler.db
      - key: SECRET_KEY
        generateValue: true
      - key: FRONTEND_URL
        sync: false
